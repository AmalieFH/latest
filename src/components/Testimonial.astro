---
const { data } = Astro.props;

console.log("mine data er ", data)

function getFirstNChars(str, length) {
  return str.substring(0, length);
}
---

<article>
  <h2>{data.title}</h2>

  <p>
    {getFirstNChars(data.description, 20)}...
    <a href={"testimonials/" + data.id}> læs mere</a>
  </p>
</article>
