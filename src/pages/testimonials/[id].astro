---



export async function getStaticPaths() {
  const url = "https://zqnsgctpvojapoxlcqhw.supabase.co/rest/v1/Royal_escape";
const api =
  "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpxbnNnY3Rwdm9qYXBveGxjcWh3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE2ODE0NTc3MTEsImV4cCI6MTk5NzAzMzcxMX0.BJhga2zaWUwoQSQsLQzv7b4yP-b13Abg0K4-uj6e_5Q";
const options = {
  method: "GET",
  headers: {
    apikey: api,
  },
};
  //const data = await fetch('...').then(response => response.json());

  const data = await fetch(url, options).then((res) => res.json());
console.log(data);

	
return data.map((singleTestimonial) => {
    return {
      params: { id: singleTestimonial.id },
      props: { singleTestimonial },
    };
  });
  return data.map((post) => {
    return {
      params: { id: post.id },
      props: { post },
    };
  });
}



  
const { singleTestimonial } = Astro.props;
import MainLayout from "../../layout/MainLayout.astro";
import Testimonial from "../../components/Testimonial.astro";
/*
{
  data.map((testimonial) => {
    return <Testimonial data={testimonial} />;
  })
}
*/
---

<MainLayout>
<h1>her er en side {JSON.stringify(singleTestimonial,null,2)}</h1>
<Testimonial data={singleTestimonial} />;
</MainLayout>
